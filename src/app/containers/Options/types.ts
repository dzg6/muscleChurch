/* --- STATE --- */
export interface OptionsState {
    member:String,
    exercise:String,
    weight:string,
    resistance:String,
    time:String,
    reps:String
}

export type ContainerState = OptionsState;
